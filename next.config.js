/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 15 uses App Router by default, no experimental flag needed
  typescript: {
    ignoreBuildErrors: true,
  },
};

module.exports = nextConfig;
